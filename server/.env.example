PORT=5000
NODE_ENV=development

# Cache duration in hours
CACHE_DURATION=24

# MongoDB connection URI (optional - if not provided, uses cache only)
# Example: mongodb://localhost:27017/malchart
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/malchart
MONGODB_URI=
